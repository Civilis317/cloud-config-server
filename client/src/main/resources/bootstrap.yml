spring:
  cloud:
    config:
      name: "configserver-client"
      profile: "local"
      label: "master"
      uri: "http://localhost:8888/configserver"
      username: "config"
      password: "welcome1"
      fail-fast: true

encrypt:
  key-store:
    location: "classpath:/config-client.jks"
    password: "welcome1"
    alias: "client1"
    secret: "welcome1"
