spring:
  cloud:
    config:
      server:
        encrypt:
          enabled: true


encrypt:
  key-store:
    location: "classpath:/config-server.jks"
    password: "welcome1"
    alias: "client1"
    secret: "welcome1"
